# Complete-Intent-Detection-Pipeline-with-GPU-Support
High-performance Persian Intent Detection service using SetFit and FastAPI, optimized for GPU inference
# 🚀 Persian Intent Detection Service (SetFit + FastAPI)

این پروژه یک راهکار کامل برای **تشخیص نیت (Intent Detection)** در زبان فارسی است. هدف اصلی این پروژه، پل زدن بین مدل‌های سنگین زبانی و نیازهای عملیاتی (Production) با تمرکز بر **سرعت پاسخگویی (Latency)** و **دقت بالا** در سناریوهای کم‌داده (Few-shot) است.

## 🌟 ویژگی‌های کلیدی
- **SetFit Framework:** استفاده از Contrastive Learning برای آموزش دقیق با داده‌های محدود.
- **Efficient Architecture:** استفاده از نسخه‌های بهینه شده مدل‌های BERT برای زبان فارسی.
- **GPU Accelerated:** بهینه‌سازی شده برای اجرا روی NVIDIA RTX 4090 با استفاده از CUDA.
- **Production Ready:** پیاده‌سازی سرویس با FastAPI و مدیریت کانتینر توسط Docker.

## 📊 بنچمارک عملکرد (Latency Comparison)
در طول توسعه، ما موفق شدیم با انتقال از CPU به GPU و بهینه‌سازی پایپلاین، زمان پاسخگویی را به شدت کاهش دهیم:

| فاز توسعه | مدل | سخت‌افزار | زمان پاسخگویی (Latency) | وضعیت |
| :--- | :--- | :--- | :--- | :--- |
| **۱. اولیه** | BERT-Base | CPU | ~ ۱۰,۰۰۰ میلی‌ثانیه | ❌ غیرعملیاتی |
| **۲. بهینه‌سازی مدل** | Mini-BERT (SetFit) | CPU | ~ ۴,۰۰۰ میلی‌ثانیه | ⚠️ کند |
| **۳. نهایی** | **Mini-BERT (SetFit)** | **RTX 4090 (GPU)** | **< ۳۰۰ میلی‌ثانیه** | ✅ ایده‌آل |

---

## 🏗 ساختار پروژه
```text
.
├── app/
│   ├── main.py          # سرویس FastAPI برای استنتاج (Inference)
│   └── train.py         # اسکریپت آموزش مدل با SetFit
├── data/
│   └── samples.json     # نمونه داده‌های آموزشی
├── Dockerfile           # تنظیمات کانتینر با پشتیبانی از CUDA
├── requirements.txt     # پیش‌نیازهای کتابخانه‌ای
└── README.md            # راهنمای پروژه
